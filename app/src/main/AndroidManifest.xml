<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Local backend testing only. Use HTTPS and remove/avoid cleartext traffic for production. -->
    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ResumeRefine">

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".SavedReportDetailActivity"
            android:exported="false" />
        <activity
            android:name=".SavedReportsActivity"
            android:exported="false" />
        <activity
            android:name=".StarBuilderActivity"
            android:exported="false" />
        <activity
            android:name=".InterviewPracticeActivity"
            android:exported="false" />
        <activity
            android:name=".BulletRewriterActivity"
            android:exported="false" />
        <activity
            android:name=".ResumeReportActivity"
            android:exported="false" />
        <activity
            android:name=".ResumeInputActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.ResumeRefine">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
